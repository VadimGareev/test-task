# Тестовое задание
Сделал все одним файлом, так как предполагаю, что скрипт нужен для миграции 1 раз, после его с проекта удалим. Если же задача требует постоянной отправки данных, например раз в сутки. То лучше делать отдельный сервис в какоим нибудь модуле и в коде использовать ORM модели для получения данных.
